name: CI/CD Pipeline
permissions:
  id-token: write
  contents: read
  attestations: write

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: docker.io
  IMAGE_NAME: tthanhhh/ai_agent_for_recruitment_website

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('AI Agent for recruitment website/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        cd "AI Agent for recruitment website"
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Set PYTHONPATH
      run: echo "PYTHONPATH=${{ github.workspace }}/AI Agent for recruitment website/backend" >> $GITHUB_ENV

    - name: Run unit tests
      run: |
        cd "AI Agent for recruitment website"
        python -m pytest backend/test/unit/mcp/test.py -v --tb=short
      env:
        PYTHONPATH: ${{ github.workspace }}/AI Agent for recruitment website/backend

    - name: Run integration tests
      run: |
        cd "AI Agent for recruitment website"
        python -m pytest backend/test/integration/mcp/test.py -v --tb=short
      env:
        PYTHONPATH: ${{ github.workspace }}/AI Agent for recruitment website/backend

    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results
        path: |
          AI Agent for recruitment website/backend/test/
  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: Test successfully
      run: |
        echo "🚀 Application successfully built and pushed!"
